DATABASE_URL="postgresql://USER:PASSWORD@HOST:6543/postgres?pgbouncer=true&connection_limit=1"
DIRECT_URL="postgresql://USER:PASSWORD@HOST:5432/postgres"
NEXT_PUBLIC_APP_URL="http://localhost:3000"
DATA_SOURCE_MODE="auto"
OPERATOR_SESSION_SECRET="change-me-to-a-random-32-char-string"

# Booking confirmation emails (optional — booking works without these).
# Get a key at https://resend.com. Leave unset to skip emails in demo mode.
RESEND_API_KEY=""
# Must be a Resend-verified domain in production. Defaults to onboarding@resend.dev.
EMAIL_FROM="Sahyadri <onboarding@resend.dev>"
